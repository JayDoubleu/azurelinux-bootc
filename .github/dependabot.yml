# Keeps the pinned action SHAs current. Each action is pinned to a commit with its version in a
# comment, and Dependabot updates both.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
