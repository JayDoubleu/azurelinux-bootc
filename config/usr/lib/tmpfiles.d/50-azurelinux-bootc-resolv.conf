# Point /etc/resolv.conf at the systemd-resolved stub at every boot.
# The build cannot create this symlink: podman bind-mounts /etc/resolv.conf during the build.
L+ /etc/resolv.conf - - - - ../run/systemd/resolve/stub-resolv.conf
