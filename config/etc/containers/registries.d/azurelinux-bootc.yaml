# Sigstore signatures travel with the image as registry attachments. This applies to every
# registry: the local one under both of its names, and a public one such as ghcr.io.
default-docker:
  use-sigstore-attachments: true
